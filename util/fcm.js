/**
 * Legacy FCM Utility Wrapper
 * Redirects to the centralized Firebase Service for backward compatibility.
 */
const firebaseService = require("../services/firebaseService");

module.exports = firebaseService;
